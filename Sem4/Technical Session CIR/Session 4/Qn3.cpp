// C++ program to find the word in a given string that has the highest number of repeated letters
#include <iostream>
#include <string>
#include <algorithm>
using namespace std;

int main(){
    string str;
    cout << "Enter a string: ";
    getline(cin, str);
    string word = "";
    string highest_word = "";
    int max_repeated_letters = 0;
    int repeated_letters =0;

    return 0;
}